name: Build ZMK firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main

board: [ "nice_nano_v2" ]
shield: [ "dchoi_left", "dchoi_right" ]
include:
  - board: nice_nano_v2
    shield: dchoi_left
  - board: nice_nano_v2
    shield: dchoi_right
